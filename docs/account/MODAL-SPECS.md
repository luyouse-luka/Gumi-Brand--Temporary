# 弹窗组件规格

> 从 29 张弹窗板逐节点量出来的**共用**组件值。Task 9 起每个弹窗只需记自己的差异，
> 共用部分查这里，不要再回头翻节点。数值全部取自 `figma/account/nodes/`，非目测。
>
> 壳的两种形态与滚动锁见 [SPEC.md](SPEC.md) §8c；每类弹窗的态清单见 `figma/account/MODALS.txt`。

## 一、壳

| 件 | 值 | 源 |
|---|---|---|
| 遮罩 | `#000000` opacity **0.6**，铺满视口 | `2284:31476` |
| 面板 | 满 **390** 宽、白底 | 全部 29 张 |
| 居中卡 | 高 ≤394，`top == bottom`，圆角 **12** 四角 | 17 张 |
| 贴底抽屉 | 高 **672** = 840−168，贴底，圆角 `[12,12,0,0]`，内部 `64 头 / 528 滚动体 / 80 脚` | 12 张 |

⚠ **桌面无稿**（待裁决 E）。用户 2026-09-10 定：**面板宽仍用板上的 390**，
其余按 account 自己的桌面语言走 —— 见下面「三、桌面差异」。

## 二、三段的共用值（手机板）

### 头 `Account Section List` — 64 高

| 项 | 值 |
|---|---|
| 内边距 | `20` 四周 |
| 排布 | `SPACE_BETWEEN`，子框 350 宽、gap 8 |
| 标题 | `16/24 w500 ls-0.32` `#1a1a1a` |
| 关闭叉 | 24×24 槽内 10×10 线条，`stroke-width 2`，`#000000` |
| 分隔线 | 底部 1px `#e6e6e6`，**`strokeAlign: INSIDE`** |

⚠ **INSIDE 的 1px 画在 64 之内，不叠加高度。** 用 CSS `border-bottom` 会让头变 65、
脚变 73，每个面板都比板高 2px。实现用 `box-shadow: inset 0 ∓1px 0`。

### 体 `Container` — 高随内容

| 项 | 值 |
|---|---|
| 内边距 | `20` 四周 |
| 底色 | `#faf9f8`（`$c-cream`） |
| 纯文案型 | 内含 `Frame 1984078400` **固定 320 宽**、左对齐（右侧余 30）；正文 `14/20 w400 ls-0.28` `#666666` |
| 表单型 | 内含 350 宽 Container，gap **8**（字段组 → 说明行） |
| 说明行 | `12/18 w400 ls-0.24` `#666666` |

⚠ 320 那个宽度**三张纯文案板完全一致**（`32621`/`33350`/`32463`），是组件不是漏改。

### 脚 `Account Section List` — 72 高

| 项 | 值 |
|---|---|
| 内边距 | `16` 上下 / `20` 左右 |
| 排布 | `MAX`（右对齐），gap **16**，垂直居中 |
| 底色 | `#ffffff` |
| 分隔线 | 顶部 1px `#e6e6e6`，`strokeAlign: INSIDE` |
| Cancel | `14/20 w400 ls-0.28` `#0374a5` **下划线**（`characterStyleOverrides`） |
| 动作按钮 | 见下 |

## 三、Input 组件 `196:17634`

整块 70 高 = 标签 20 + gap **6** + 控件 44。

| 项 | 值 |
|---|---|
| 标签 | `14/20 w400 ls-0.28` `#666666` |
| 控件 | 350×**44**，内边距 `10/14`，gap 8，圆角 **8**，白底，描边 `#cccccc` 1px |
| 值文字 | `16/24 w400 ls-0.32` `#666666` |
| 尾部图标 | 20×20 槽；日期是日历 `350:19032`，频率是箭头 `30:862` |
| 前置 icon / mail / help | 三个槽在板上全是 `visible=false` |

内容框宽度：无尾图标 **322**、有尾图标 **294**（322 − 8 − 20）。

## 四、动作按钮（Button 组件）

| 项 | 值 |
|---|---|
| 高 | **40**（不是卡片 CTA 的 44） |
| 内边距 | `14 / 32` |
| 圆角 | `10000`（胶囊） |
| 宽 | **抱内容**：`Save` 101 / `Skip next order` 186 / `Get it to me now` 196 / `Update payment method` 264 |
| 可用态 | 底 `#005635`，字白 |
| 禁用态 | 底 `#e6e6e6`，字 `#808080` |
| 字 | `16/24 w500` **`ls 0`** |

⚠ **`ls 0`，不是同尺寸别处的 `-0.32`** —— 按钮组件自己把字距归零了。

⚠ **禁用态是便签 `27446` 的脏值门控**：有表单的三个（`edit-name` / `edit-date` /
`edit-frequency`）板上就是灰的；确认型的三个（`skip-next` / `need-now` / `edit-payment`）
没有可改的东西，板上是绿的、直接可用。

## 五、桌面差异（唯一一处）

board `2284:27792` 证明 **account 桌面与手机共用同一套字号阶梯** —— 标签 `14/20`、
按钮 `16/24`、小链接 `12/18`、页标题 `24/30` 全都一样。Task 6 那个 `1.1554`
只作用在四个日期块上，不是全局缩放。

所以桌面唯一要变的是**容器内边距**，按 account 自己的卡片惯例：

| 元素 | 桌面 | 手机 | 中间 |
|---|---|---|---|
| 头 / 体 / 脚的**左右** | `24` | `20` | `fluid(20px, 24px)` |
| 体的**上下** | `24` | `20` | `fluid(20px, 24px)` |
| 头的上下 | `20` | `20` | — |
| 脚的上下 | `16` | `16` | — |

对照：`.gb-acct-sub__body`（24/20）与 `.gb-acct-sub--detail .gb-acct-sub__head`（24/20 仅左右）。

## 六、本轮未做 / 待确认

- **`edit-frequency` 的下拉用了原生 `<select>`**，没按 PLAN 重画一份自定 listbox ——
  板上**只有闭合态**，展开态无稿，自画等于自拟视觉。原生 `<select>` 与闭合态逐值一致，
  且键盘与读屏行为免费。要自定 listbox 需先给展开态的稿或同意照搬现站 `.gb-select`。
- **纯文案体的 320 固定宽**照板实现，右侧留 30 空。若那其实是漏改，改成 350 即可。
- 板上错字两处已登记 SPEC §8：`wan to skip`（`32775`）、`Restart subscoption`（`34192`）。

## 七、卡片框 `Frame 1984078395`（r-a10 起）

地址卡（`2284:32448`）与成功提示（`2284:32932`）是同一个框，只换填色：

| 项 | 值 |
|---|---|
| 尺寸 | 350 宽，高抱内容 |
| 内边距 | `16` 四周 |
| 圆角 | **8** |
| 描边 | 1px，`strokeAlign: INSIDE` |
| 排布 | `HORIZONTAL` + `SPACE_BETWEEN` |
| 地址态 | 白底 `#ffffff`，描边 `#cccccc` |
| 成功态 | 底 `#f6feec`，描边 `#daf6b0` |

⚠ **这个框的 `strokesIncludedInLayout` 是 `true`** —— 和头/脚的发丝线相反：
`16 + 内容 + 16 + 1 + 1` 才等于板上的 178 / 78。所以这里要写**真 `border`**，
不是 `box-shadow: inset`。头脚那两条仍必须用 inset，两处不能互抄。

⚠ **`SPACE_BETWEEN` 会吞掉 `itemSpacing`**：地址卡写着 gap 24，实测两个孩子之间只剩
9.8 —— Figma 的 SPACE_BETWEEN 根本不读 itemSpacing（头 `Account Section List` 的 g33
同理）。把 24 当成真 gap 写进 CSS，文字列会被挤窄 12px 并折行。

## 八、单行截断

`2284:32458`（地址卡的 delivery instructions）是这批板里**唯一**带
`textTruncation: ENDING` + `maxLines: 1` 的文本。判据：`absoluteRenderBounds` 的
宽度（240.99）远小于同串在浏览器里的自然宽度（278.6），而其余五行两边差 <1px。
不做截断，卡片就是 198 而不是 178。

其余 TEXT 节点都没有 `maxLines` —— 加之前先查这两个字段，别一律 clamp。
