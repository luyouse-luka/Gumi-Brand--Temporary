# Gumi Brand — 项目状态

> 建档 2026-08-19。本文件只放**项目定位、约定、仍未关闭的待决事项**。
>
> | 要做什么 | 先读 |
> |---|---|
> | 接手任何一轮 | [HANDOFF.md](HANDOFF.md) —— 不要报成 bug 的清单 / 验证跑法 / 待决索引 |
> | 查历史改动 | [CHANGELOG.md](CHANGELOG.md)（近 10 轮）+ [CHANGELOG-ARCHIVE.md](CHANGELOG-ARCHIVE.md)（第一～三十轮）；**两份一起 grep** |
> | 查设计节点 | [FIGMA-NODES.md](FIGMA-NODES.md) / [DESIGN-TOKENS.md](DESIGN-TOKENS.md) / [HANDOVER-NOTES.md](HANDOVER-NOTES.md) |
> | 查逐轮进度与已关闭的待决 | [PROJECT-STATUS-ARCHIVE.md](PROJECT-STATUS-ARCHIVE.md) |

## 项目定位

Shopify 前端项目（Gumi Brand 官网 + 商品页）。设计交付源为 Figma
`Internal - Gumi Brand & Website` 的 SECTION `401:31719`「Desktop & Mobile MVP (14/08/26)」，
位于 **🟢 READY FOR DEV** 分组下。

**当前阶段：11 个静态页全部落地，四线审计的上线阻断级问题已修完**（Homepage / PDP / Science / Reviews / How Gumi Works /
Our Story / FAQ / Get in Touch / Referral / Privacy Policy / Shipping），
每页 5 个宽度（390/768/1024/1440/1920）无横向溢出、无卡住的入场动画。
改动记录见 [CHANGELOG.md](CHANGELOG.md)。

验证脚本 `tools/shoot.py`（全页截图 + 溢出 + 卡住的 `.wowo`），`python3 tools/shoot.py --all`。
新页面外壳由 `figma/new-page.py` 从 index.html 现切现拼，`--resync` 可把 header 改动推给所有页。
长文案一律用 `figma/dump-text.py` 从节点取全文 —— `build.txt` 会把长字符串截断成省略号。

⚠ **两端要同步做**：手机稿（390）不是桌面稿的等比缩小，数值必须各取各的。第四轮抽查发现
Homepage 有 3 处 mobile 字号是凭手感缩的（已修）。判据是把每个 `@include mobile` 块里的
字号与手机稿实际字号集合对一遍 —— 不在集合里的就是编的。

⚠ **交互态要一起做**（CLAUDE.md 铁律 13）：可点击的地方都要有 hover，状态变化都要走
`transition`。这类要求**既不在 Figma 稿里也不在 build spec 里，没人提就一定漏** —— 第五轮
清点时 19 类可点击元素只有 9 类写了 hover、其中 7 类还没过渡。时长与曲线统一在
`_variables.scss` 的 motion 段（`$t-base` / `$ease-out`），hover 一律用 `@include hover`
包 `@media (hover: hover)`，否则触摸屏点完 hover 态会粘住。
判据是 Playwright 遍历所有 `a[href]` 与 `button`，比对 hover 前后的 computed style。

## 待用户确认的事项

### 开工前必须定的
1. **店铺与主题** — Shopify 店铺域名、主题基底（Dawn？既有主题？）、是否已有 dev 主题
2. **实现形态** — 直接写 Shopify 主题（sections/snippets/liquid），还是先出静态 HTML 再套主题
3. **接入方式** — Shopify CLI（本机需走 device code 流，见 memory `shopify-cli-headless-device-code`）/ GitHub 集成 / 手工上传
4. **PP Palma 授权文件** — 三个免费字体（Inter / Lexend / Playpen Sans）已下载到位；
   **PP Palma 是 Pangram Pangram 商业字体，试用包 EULA 明确排除商业用途**。
   ⚠ **第六轮起已接入试用包**（Figtree 退为 `$font-brand-alt` 兜底，不再是主字体）：
   300/500/800 用包内 Fizzy 三个原件；**400（全站 59% 用量）试用包不提供**，
   第十一轮起指向 `fizzy-light`，即 **400 与 300 同款、正文比稿轻一个字重档**
   （插值件在客户浏览器上渲染成 last-resort monospace，已弃用；详见 CHANGELOG 第十一轮）。
   **需要客户提供授权 web font 文件**才能做像素级还原验收，切换点只有 `_fonts.scss` 一处。

### 需要设计方给数值的
- **交互态（hover / active）稿里完全没有** — 现有的抬起量（1px 按钮 / 4px 卡片）、投影、
  时长（.15 / .2 / .3s）与曲线（easeOutCubic）都是自定值，集中在 `_variables.scss` 的
  motion 段，要调改那一组即可。
- ~~手风琴与 tab 的**展开态图标**该变成什么形态~~ — 稿里仍然没有，但需求方 2026-08-24
  在任务文档里指定了：**加号的竖线旋转成横线**（即减号），且不用 SVG、用 CSS 画。已落地。
- tab（营养标签弹窗的两个页签）的选中态仍是自定值。


### 桌面稿与手机稿对不上的地方（第十二轮发现，**需设计方裁决**）

四处两稿冲突，一律取信息量更大的一版，没有自造内容：

| 位置 | 桌面稿 | 手机稿 | 现在的做法 |
|---|---|---|---|
| Science 三张 stat 卡 | 三张同一句占位 | 三段各不相同的真文案 | 用手机的文案；数值仍取 95%（桌面 + homepage 一致，手机的 50% 判为占位残留） |
| Science 成分区收尾 | 「Shop Now」按钮，标题 Heading | 四行手风琴，标题 Just the necessities | 两套都做，按断点切换 |
| Science nutrient 卡 | 3 张 | 4 张 | 做 3 张，不造第 4 张 |
| How Gumi Works 副标 | This is a placeholder subheading. | 真文案，且是珊瑚红 #dd655e | **2026-08-25 需求方定：用桌面的占位串**（第三十轮改，此前用手机的真文案），颜色仍各按各稿 |

### 第十四轮新增的待决事项

- **`gumi-bear-front-glow.png` 是首页 LCP 图，源只有 528px 宽**（对 439 CSS px 是 1.20×，
  2× 屏上会发虚）。**需向设计方索取更高分辨率的源**，放大现有文件解决不了。
- **`bear-gummy-glow.png`（767 KB）全站零引用** —— 是废弃资产还是漏接的图？未删，待确认。
- **手机稿里没有白色 promo 卡**，它的手机 `gap` 取 12 还是 24 需设计方定（现按 12）。
- **Privacy / Shipping 两个手机稿的页脚是另一版组件**（16/24/600/ls 0），与其余 9 稿冲突。
  本轮按 9 稿的 14/20 做，**先定这条才能继续动页脚**。
- index 的 24 个 media logo 仍是 `loading="eager"`：怀疑跑马灯靠它测宽，改前要先确认。

### 第三十六轮新增的待决事项（2026-08-26）

- **shipping / privacy-policy 两页的手机稿里没有 footer CTA 区块**，另外 9 页都有
  （`Footer Section CTA` 1504 / 1564）。实现这两页都挂了 CTA。删掉一个 CTA 是内容与
  转化决策，不是还原，**未动**。这两页的页脚本来就有一条挂着的冲突（第十四轮：它们用
  的是另一版页脚组件 16/24/600/ls 0），大概率是同一批稿的模板差异，请需求方一并定。
- **shipping 两个表的行高差 8px**：按稿设完列宽后是 384 / 360 对稿的 376 / 352，差额是
  表头加 7 行共 8 条 1px 分隔线 —— 稿里那是不占布局高度的 RECTANGLE，实现用的是
  `border-bottom`。要逐位对上得改成绝对定位的伪元素，收益很小，未动。
- **index hero 标题与 `Clean ⏎ Ingredients` 没有硬断兜底**：两处都靠宽度折出了与稿相同的
  行数（前者来自 `.gb-hero__title` 的 `padding: 0 25px`），换上授权版 PP Palma 后要重验。

### 第三十七轮新增的待决事项（2026-08-26，全站截图逐区块对稿）

**A. 桌面稿与手机稿的文案冲突 —— 实现一律取的是桌面版，手机稿另有一版**
（沿用既有原则「取信息量大的一版」，但这六处此前没有记录，需求方要么确认、要么指定按断点切换）：

| 位置 | 手机稿 | 桌面稿 = 现在的实现 |
|---|---|---|
| PDP 第二张 promo 卡 | `No nasties` / 按钮 `Discount xx` | `No nasties, full stop` / `Shop Now` |
| Science `.gb-compare` 引导句 | 「…absorption earlier which leads to fast and efficient nutrients into the bloodstream.」 | 「…absorption earlier in the digestive process. This leads to fast and efficient bioavailability (delivery of nutrients into the bloodstream).」 |
| FAQ 页 CTA 板 | `STILL HAVE QUESTIONS?` / 「Can't find the answer you're looking for? Please chat to our friendly team.」（标题 24/30）/ 按钮 `Contact us` | `GET HEALTHY` / 「Join the 10% of people who are nutrient sufficient」（标题 30/36）/ `Start Your Greens` |
| Referral 表单按钮 | `Sign up` | `Send Message` |
| Referral 登录行 | `Sign in`（小写 in，带下划线） | `Sign In` |
| Reviews 页 FAQ 区标题 | `FAQ's` | `And Last Questions?` |

⚠ **Referral 那条有旁证**：同一块里的「Already have an account? Sign in」是注册语境，
桌面的 `Send Message` 与 get-in-touch 的按钮一字不差，像是复制未改。建议改成 `Sign up`，
但那要动桌面，**未改**。
⚠ **FAQ 那条的副作用**：`Start Your Greens` 在手机 270 宽的按钮里会折成两行（手机稿的
`Contact us` 不会）。文案不换也该单独处理这个折行。

**B. 桌面稿与手机稿的占位数量冲突 —— 都是灰色占位，真实数量由 Shopify 数据决定，未改**

- **产品图缩略图**：手机稿 **6** 个（52×52，间距 7.6），桌面稿 **5** 个（间距 4.56）。实现是 5。
  影响 index / pdp / reviews / how-gumi-works / our-story 五页。补到 6 会让桌面能滑到一张
  看不见的第 6 张（`gallery` 用 `slides.length` 定边界），得同时改 JS，收益是一个占位块。
- **FAQ 页手风琴条数**：手机稿 **8** 条（1 open + 7 closed），桌面稿 **6** 条。实现是 6。

**C. 稿自身的 WIP 痕迹（判定不追，列出来备查）**

- PDP「Batch Tested Quality」在两块板上都是**红色 `#ff2d55`**，同排的另外两项是深色；
  「Tastes Like」下面三个 `Heading` 同样是红色。红色在这份稿里像是设计师的待办标记。
- FAQ 第一条手风琴的展开内容，手机稿写的是「First accordion open on the FAQ page」
  （读着像给开发看的说明），桌面稿是 `Text here`。实现用 `Text here`。

**D. 两端同源、改了会动桌面，所以没动**

- **`.gb-vs__row` 的行距每行多 2px**：稿是「行 → 12 → 0 高的 Line → 12 → 下一行」共 24，
  实现是 `margin-top 12 + border 1 + padding-top 13` 共 26。两块板都是 24，桌面同样偏 2，
  5 行累计 8~10px。改成 `11.5 / border / 11.5` 两端都会动。
- **Reviews 专家卡轮播的初始位置**：手机稿画的是**第二张居中**（左右各露出一截），实现是
  从第一张开始（rail 左对齐）。`slider` 支持 `data-slider-centre`，没启用 —— 轮播从第一张
  起是常规行为，稿画的更像是滚动中的展示态。

### 第三十八轮新增的待决事项（2026-08-27，响应式一批）

**E. footer 链接区：两块板之间的结构分歧，实现取的是分组版**

板 `I236:12187;236:10665;187:4010`（手机）把 13 个链接排成 **两列、没有小标题**
（159 宽，左列 6 条 / 右列 7 条，列间 32）；实现是 **三组带小标题**
（`Why Gumi` / `Learn more` / `Get in touch`，每组 4 条），narrow 下走 2 × 2 网格。

- 后果：这一块 **328 高对板的 212，多 116**，Follow us 与版权行跟着整体下移。
  第三十八轮把 footer 另外四个块调到逐像素对板（139.00/140/68/89 对 139.04/140/68/89），
  **这是唯一剩下的差**。
- 桌面板的链接列同样是分组的（`285:18162` 的 `_Footer links column` 各 194 宽），
  所以改成两列会让桌面和手机对不上，也会丢掉分组语义。**未改，请需求方定**：
  ① 保持分组（手机端接受这 116）② 手机端改成两列无标题 ③ 手机端把三组压成两组。

**F. `.gb-footer` 的 padding-bottom：需求方给 24，板是 48**

`187:3984` 是 `paddingTop 64 / paddingBottom 48`。第三十八轮 padding-top 落 52
（= 64 − 小波浪多出的 12，与 `.gb-science` 同源，波浪修好后回到 64），**而 24 没有板上的出处**，
是需求方对着截图给的。已照落，列出来备查。

### 第三十九轮新增的待决事项（2026-08-27，手机菜单改版一批）

**G. ~~`font-check.html` 有两条断言从第十九轮起就恒假~~ — 第五十二轮已改写，
连带清掉另外两条没人登记的 ✅**

「波浪归属：section 自带下边缘形状」与「裁切型宿主也不用特例：占位块在 padding box 内」
两条都在探 `.gb-product::after` 的占位块。第十九轮把留高度的方案从 `::after` 换成了
`padding-bottom: calc(既有值 + var(--sc-h))`，`::after` 随之不存在（`content` 现在读回
`none`），两条从那时起就一直报「不在（旧文件）」。

- **不是第三十九轮引入的**（该轮只给 `.gb-product--page` 加了 padding-top）。
- 一个恒假的断言和恒真的一样有害：它训练人忽略那张表上的红色。
- 等价的新判据是「`padding-bottom` 里含 `var(--sc-lg-h)`」，五行可改写。与当轮任务
  无关所以没动，**请示下是否一并更新**。

> **第五十二轮已改写，本条关闭。** 复查时发现**实际是四条不是两条**，且这里的归因不全：
>
> - 「裁切型宿主」那条**与 `::after` 无关** —— 它要求 `padding-bottom` 恰好是 0，
>   而 `.gb-nutrition` 实测是 127.979px：裁切型宿主走的正是同一套 padding 留高度的机制。
> - 另外两条从来没被登记过：**「reels 拖动态关掉 snap」**（第五十轮全站轮播改 Swiper 后，
>   `.gb-reels` 的 `overflow` / `scroll-snap` / `is-dragging` 全撤了）与
>   **「产品图库改叠放淡入」**（第四十九轮把堆叠与淡入交给 Swiper，slide 上再也
>   看不到 `opacity` 规则）。
>
> 四条全部改成验新机制，并逐条做了活性自检（破坏对应机制，四条各自且只有自己报红）——
> **把恒假改成恒真同样有害**。改完 65 行的自检表一条红都没有。

**H. ~~reviews.html 还留着一个 `.gb-app-slot`~~ — 已关闭（第四十一轮）**

需求方 2026-08-27 点名去掉，已删。删的是那个虚线占位框，`.gb-app-section` 与
「Real Customer Reviews」标题保留（评论 app 接进来时挂在这一节里）。`.gb-app-slot`
的两条 scss 规则随之零引用、一并删掉。⚠ `.gb-product__app-slot` 是另一个类，
四个页面仍在用，没动。

**I. ~~`.gb-promo-card__list` 的 `margin-right: 15px` 实际效果是靠右，不是左移~~
— 第五十一轮已裁决：去掉 `margin: 0 auto` ✅**

base 是 `margin: 0 auto`；右边一旦固定，左边的 `auto` 就吃掉全部余量，元素贴到距右边
15px 处。实测 390 下左 24.39 / 右 15。按需求方给的字面落的，视觉上略偏左于版心、看着
合理，但如果原意是「居中之后整体左移 15」，要改成 `margin-left: 15px` 配 `margin-right: 0`
（或给父级加 `padding-right`）。

> **裁决（第五十一轮任务文档第二组第 7 条）**：「767px 以下去掉 `margin: 0 auto`」。
> 落地方式是 `narrow` 档补 `margin-left: 0`。没有 auto 外边距之后，父级
> `.gb-promo-card__stack` 的 `align-items: center` 接管，右边那 15 把盒子推到
> **中心偏左 7.5** —— 正是板上「hangs slightly left of centre」的样子，
> 方向从此前的偏右修正了过来。**本条关闭。**

**J. ~~两栏堆叠阈值:991 与「推到 1200」方向相反~~ — 第四十二轮已定向：767**

需求方在任务文档第 5 条与第 8 条里两次点名要 **767 以下才堆叠**（第 8 条还特别写了
「gb-ingredients__inner … 发现没有修改成功」「gb-compare__inner 也是如此」）。三次改动
都在往「更晚堆叠」推：1024 → 991 → 767，**第二十九轮那条「推到 1200」就此作废**。
`.gb-product__inner` 也一并跟到 767。下面是原文备查。

**J. 两栏堆叠阈值:991 与「推到 1200」方向相反,请确认哪条作数**

第二十九轮的遗留写的是「两栏堆叠阈值仍是 1024,**没有按需求方说的推到 1200**」——
推到 1200 意味着**更早**堆叠(1200 以下就变上下)。第四十轮需求方要的是
**991 以下才堆叠**,即 992–1024 恢复并排,是**更晚**堆叠。两条不能同时成立。

- 本轮按最新的 991 落地。配套去掉了 `.gb-compare__heading` / `__panel` 的 560 上限、
  把两个正方形图块从 `flex: 0 0 520px` 放开成 `flex: 0 1 520px`,所以 992–1024
  的两栏是撑得住的:compare 368.8 / 466.8,ingredients 437.8 / 421.8,faq-image 同。
- 涉及 `.gb-compare__inner`、`.gb-ingredients__inner`、`.gb-faq-image__inner` 三处
  (science 页两处 + reviews 页一处)。
- **若第二十九轮那条仍然有效**,改回只需把这三处的 `@include mid` 换成 `@include tight`
  (1200)或 `@include stack`(1024),同组的六条配套规则要一起换。

### 第四十一轮新增的待决事项（2026-08-27）

**K. footer 链接区的对齐已经反转两次，当前是第三次落法**

| 轮次 | 落法 | 谁提的 |
|---|---|---|
| 第二十轮 | `stack`(≤1024) 覆盖成 flex-start | 我们（换行后左边空 260 的洞） |
| 第三十八轮 | 删掉覆盖，1280 以下全部 flex-end | 需求方点名 |
| 第四十一轮 | `tablet`(768–1280) 覆盖成 flex-start | 需求方点名 |

当前落在**值档 `tablet`**（上界 1280 正是需求方两次说的数字），不是布局阈值 ——
PROJECT-STATUS「断点体系」有一条明确的 ⚠「布局阈值不要去对齐 767/1280」。
**若再动这处，请一并说明与前两次的关系**；回退只需删掉那一条 `justify-content`，
基础规则本身就是 flex-end。

**L. `.gb-app-section` 现在只剩一个标题（删掉占位框的直接后果）**

reviews 的「Real Customer Reviews」这一节高度从 656(1440) / 427.3(390) 降到
368 / 199.3，标题下方直接接波浪。这是需求方点名要的，但**在评论 app 接进来之前**，
页面上会看到一节几乎空的区块。若观感上不接受，三个选择：
① 保持现状等 app ② 整节先隐藏、接 app 时再开 ③ 换成无边框的空容器占住高度。

### 第四十二轮新增的待决事项（2026-08-27）

**M. 去掉宽度上限后，两个正方形贴边了 —— 已于第四十三轮由需求方裁决关闭 ✅**

需求方点名去掉 `.gb-ingredients__disc` / `.gb-faq-image__media` 的 `max-width`
（第四十轮保留 520 上限被判为「没修改成功」）。按字面落地后实测：

| 视口 | 现在 | 此前 |
|---|---|---|
| 390 | **390 × 390，左右贴边** | 350 × 350，各留 20px 版心 |
| 767 | **767 × 767，占满整屏** | 350 × 350 |

根因是 `.gb-ingredients__inner` 在 narrow 档是 `padding-inline: 0`（留白由 `__body`
自己补），所以一去掉上限就贴边。**若原意是「不要固定像素、但保留版心留白」**，
一行可改：inner 的 narrow 档改成 `padding-inline: var(--pad-x)`，`__body` 的
补偿相应去掉。请需求方定。

> **裁决（第四十三轮任务文档第 8/9 条）**：按上面这行预案落地 —— 版心回到 `__inner`，
> `__body` 的 padding 去掉，两个正方形各加 `max-width: 520px`。现在 390 处 350×350、
> 767 处 520×520 居中，正文起点仍是 20。**本条关闭。**

**N. `.gb-stat__value` 要不要也加数字增长**

第 7 条「给这些数字增加数字增长动画,比如 gb-science-card__value」——
已挂在 `.gb-science-card__value`（9 处）。首页四个大数字 `.gb-stat__value`
（60+ / 6g / 21 / 10+）**没挂**：它是 `.gb-ink-halo` 描边复制层 + 真实内容的双层结构，
且已经挂着 `data-line-reveal` 行揭示，两套动画落在同一节点上要先定先后顺序
（描边层与内容层还必须逐帧同步，否则描边会跟不上数字）。
`.gb-vs__value` 是文字（"Actually good" 之类），不适用。

### 第四十三轮新增的待决事项（2026-08-27）

**O. `.gb-dosed__media` 手机端仍停在 350，与放开后的正文列不相称**

第 5 条说「去掉 media 固定宽度」，去掉的是桌面那条 `width: 598px`；手机端的
`max-width: 350px`（稿在 390 的值）本轮**没动**，因为第 9 条点名加 520 上限的两个模块里
不含 dosed。后果是 767 处正文列已放开到 727，而正方形还是 350，视觉偏小。

要比照 ingredients / faq-image 处理的话，一行：`.gb-dosed__media` 的 narrow 档
`max-width: 350px` → `520px`。**是保留稿值还是跟齐 520，请需求方定。**

**P. expert 轨道的居中只落到 767 以下**

第 2 条写的是「手机端居中」，本轮按字面落在值档 `narrow`；768–991 这一带轨道仍是
`start` 对齐（那里本来就露 2.3 张，提示够用）。**若原意是整条轨道都居中**，
把 `.gb-expert-card` 里那行 `scroll-snap-align: center` 从 `@include narrow` 挪到
`@include mid` 即可，无其它连带。

**Q. 第 6 条的「media 最大宽度」没给数值，本轮取 520**

需求写的是「去掉 inner 的 `max-width: 560px`，给 media 加最大宽度」，没给数字。
本轮取 **520 = 560 − inner 自己的 20+20 padding**，所以 `.gb-product__media` 在任何宽度下
与改前**逐像素相同**，被放开的只有正文列。数值来自被移除的那条规则，不是新拟的 ——
**若本意是别的数字，请给一个。**

**R. ~~任务文档第 10 条正文为空~~ — 已于第四十四轮补齐并完成 ✅**

`修改任务文档.txt` 末尾曾有一行只有编号「10」、没有内容。需求方随后**追加**了正文
（dosed 标题多空格）以及 11/12/13 条，第 10–12 条第四十四轮已做。

### 第四十四轮新增的待决事项（2026-08-27）

**S. ~~第 13 条（`gb-cta-band` 背景形状变形）~~ — 已于第四十五轮解决 ✅**

已实测量化。`.gb-cta-band__plate` 的遮罩是 `center / 100% 100%`，也就是**拉伸**，
而同一段代码的注释写着「Desktop and mobile are SEPARATE shapes … 
**Each is used at its own board's size, never stretched**」——实现与写下的意图是矛盾的。

| 视口 | 板尺寸 | 宽高比 | 该档用的稿比 | 拉伸倍数 |
|---|---|---|---|---|
| 390 | 350 × 507.5 | 0.690 | 0.691（手机稿） | **1.00×** |
| 575 | 535 × 507.5 | 1.054 | 0.691 | 1.52× |
| **767** | 727 × 507.5 | 1.433 | 0.691 | **2.07×**（顶底 5 个圆瓣被拉成扁椭圆） |
| **768** | 728 × 507.5 | 1.434 | 3.257（桌面稿） | **0.44×**（顶底 14 个圆瓣被压成竖椭圆） |
| 1024 | 924 × 449.8 | 2.054 | 3.257 | 0.63× |
| 1280 | 1120 × 392.2 | 2.856 | 3.257 | 0.88× |
| 1440+ | 1280 × 392 | 3.265 | 3.257 | **1.00×** |

即：**只有两块设计稿自己的宽度是准的，中间整段都在变形，且 767/768 的缝两侧朝相反方向变形。**

根因是结构性的，不是一行能改的：两块稿是**两张不同的画**（14 个瓣 vs 5 个瓣、
圆半径 58.88 vs 39.92、瓣间距 90.1/91.7 vs 67.8/61.1），而板的宽高比从 0.69 连续变到 3.26。
一张固定的闭合轮廓被拉到任何非原生比例上都会变形，**换用哪一张都救不了中间段** ——
767 处 727×507.5 这个比例离两张稿都差一倍以上。

**我没有擅自选一个方案**，因为三条路都是设计决策而非还原（全局铁律 20）：

1. **不拉伸，让瓣数跟着盒子走**（与站内波浪 `--sc-w` 同一原则：瓣的画法固定、
   宽了就多重复几个）。忠于「never stretched」的原意，代价是要把闭合轮廓拆成
   「内矩形 + 四条边的圆瓣平铺」共约 9 层 mask 合成，且瓣的**重叠量**（稿里瓣间距小于直径，
   是叠着的）在平铺下无法保持恒定，末端会有一处接缝。
2. **限制板的宽度**，让它在中间段保持接近稿的比例（例如 767 以下板不铺满版心）。
   一行就能改，但这是把「通栏的板」改成「居中的卡片」，是版式决策。
3. **接受变形，只把最难看的缝抹平**：把两张稿的切换点从 767/768 挪到比例交叉处，
   并让板的高度更早地往桌面稿的比例收。改动小，但中间段仍然变形（约 0.5×–0.7×）。

请需求方选一条，或直接出一版 768–1280 的设计稿。

> **裁决与落地（第四十五轮）**：需求方回「想办法解决，不行就换 svg 图或者其他办法」。
> 结果三条都没用上 —— 把两条 `fillGeometry` 路径解开后发现**两块稿是同一种构造**
> （半径固定的圆瓣按固定间距排开，与内矩形取并集），于是改成**九宫格 `border-image`**：
> 四角按原尺寸绘制永不缩放，四边各用 `round` 平铺一个瓣周期，中间纯色拉伸无影响。
> 纯 CSS、不换图、不依赖 JS、布局零影响。
>
> 横向畸变从 **0.44–2.07 倍** 收到 **0.94–1.02**，纵向 0.87–1.07（`round` 在周期数
> 只有 3–4 个时的固有粒度）；390 与 1440 两块稿的瓣数（顶 5/14、侧 8/4）**完全复现**。
> 判据 `tools/platecheck.py`。**本条关闭。**

**T. 第 11 条的 `gap: 80px` 没写档位，本轮按基础档落**

需求写的是 `.gb-dosed__inner  gap: 80px;`，没有 `手机端` 之类的限定 —— 同一份文档里
凡是手机端的条目都明写了。所以按「规则自身的值」= 基础档（板是 96）落，`tablet` 斜坡
上端同步到 80，手机档的 48 未动。**若原意是别的档位，说一声即可。**

**U. `.gb-page-hero__title` 在 1281 附近折成 5 行（顺带发现，未修）**

1281 处 hero 的文字列只有 486.8 宽而标题是 64px，"Aussie-approved." 这一个词在
连字符处自己断开，标题从稿上的 3 行变成 5 行（science / reviews / our-story 都有）。
1440 处列宽 566 就放得下。成因是 `.gb-page-hero__media`（`1 1 570px`）与
`.gb-page-hero__text`（`1 1 566px`）在 1281 同时收缩 —— 1281 的版心内容只有 1061，
两栏加 84 的 gap 要 1220。与本轮无关，也不是第 10 条那个病。

可选改法：pc 档把 `.gb-page-hero__inner` 的 gap 也做成斜坡，或给 `__text` 一个更小的
basis 让 media 先让。**都是取舍，等裁决。**

### 第四十七轮新增的待决事项（2026-08-28）

**V. CTA 按钮标签折行的真因是 `__content` 的 gutter，本轮只治了标；请定要不要治本**

第 15 条写的是「`.gb-cta-band__btn` `padding: 0 64px` 需要加响应式」，按字面在 `narrow`
档收到 24 就够了（标签在 320–390 不再折行）。但**板上按钮的内缩本来就是 64**：
`I324:53922;236:11728` 是 350 宽的 STRETCH 按钮，64 的内缩留 222 给内容（icon 24 +
gap 8 + 标签 146 = 178，宽裕）。实现装不下，是因为按钮只有 274：

| | 板 `Frame 992505` | 实现 `.gb-cta-band__content` |
|---|---|---|
| 左右 gutter | **0**（内层文字帧 `Frame 992504` 才有 8） | **38** |
| 按钮宽 | 350 | 274 |
| 按钮内可用 | 222 | 146（标签墨迹 146.34 → 折行） |

治本的改法是把 `__content` 的手机 gutter 收到 0、按钮回到 350、内缩回到板的 64。
**这会同时放宽标题与引导句**（现在 274，板上是 334），可能改变它们的折行，
所以没有擅自动 —— 那是版式而不是这一条需求。请定：保持现在（标 24 / 板 64），
还是回到板的构造。

**W. 两个 `--center --lg` 页头桌面各矮了 26px（第 15 条的直接代价）**

按需求把 `--lg` 的净留白统一到 `--center` 的 70/64 之后，how-gumi-works 与 our-story
在 1440 处内容底到波浪顶从 96 收到 70，页面各矮 26（实测 7812.7 → 7786.7、6934.5 → 6908.5）。
这正是第三十七轮记的那条遗留（「若 70 是对所有居中页头都成立的，`--lg` 那条要一起改」）
的裁决落地。**若 96 才是这两页要的，把 `.gb-page-hero--center` 的 70 与 `@include tablet`
的 `fluid(64px, 70px)` 改回 96 / `fluid(64px, 96px)` 即可，一处生效。**

⚠ **`.gb-page-hero--lg` 这个 class 没有删掉，也删不掉**：两页 hero 里挂的是
`gb-scallop--lg`（1440 处实测高 128.97，小瓦片是 96.94），section 读不到自己子元素的
波浪尺寸，这个方向的信息只能靠 class 传。真删掉、padding 退回 `--sc-h`，内容到波浪的
距离会从 96 掉到 37.97。现在它只剩一行 `--hero-wave: var(--sc-lg-h)`，不带任何数值。

**X. 富文本段距只改了手机端，桌面仍是 20（板是 16）**

第 18 条点名手机端，已落 `narrow` 16 + `tablet` `fluid(16, 20)`。但板上
privacy + shipping 两页 **103 个** 16/24 正文节点的 `paragraphSpacing` **全是 16**
（第三十三轮遗留第 1 条量过），桌面那一档的 20 是第三十轮自己定的。
**要不要一并改成 16？** 改法是一个数（`.gb-rich-text` 那六个选择器的基础 `margin-bottom`），
代价是 privacy 与 shipping 两页的纵向节奏整体收紧。

**Y. shipping 表格的网格现在在所有宽度都画，768+ 没有稿背书**

第 19 条之前，整套网格（fixed 布局 / 描边 / 表头与斑马填充 / 9.5-12 内边距 / 44 行高）
只写在 `narrow` 里，768 一过就掉回自造的桌面 fallback（auto 布局、只有一条 border-bottom、
行高 49、列宽 181/547），与板毫无关系 —— 这是本轮判定为缺陷并修掉的。
**但 shipping 没有桌面板**，「768 以上也画同一张网格」是我按「唯一画出来的表就是它」
做的判断。列宽的处理同理：板里一列 HUG 一列 FILL，所以固定列写 px（表 1 的 88、
表 2 的 147），350 板宽处精确落在 88/262 与 203/147，更宽的视口只长 FILL 那一侧。
**若设计方另有桌面表格版式，这一整块要重做。**

⚠ 另有一处**没动**：板的描边是 `#cccccc 0.5 CENTER`，实现写 `0.5px`；headless 在 DPR 1
下会上舍入成 1px，DPR 2 的真机则按 0.5 画。要不要钉死成 1px，等真机确认。

### 第四十九轮新增的待决事项（2026-08-28）

**Z. ~~footer CTA 按钮 hover 之后底色和它脚下的底板一模一样~~ — 已于第五十轮由需求方撤回 ✅**

第 6 条要求把 `.gb-footer-cta__btn` 的 hover 换成 `.gb-btn--primary gb-header__cta` 的那套
（`background: $c-lime; color: $c-green-900`）。问题是 `.gb-footer-cta` 整块底板就是 `$c-lime`
（实测 `rgb(181,237,97)`）—— hover 之后按钮底色与底板完全相同，**按钮的形状只剩基础规则那
1px 绿边框在撑着**。原来的写法（翻成白底）正是为了避开这一点，源码里当时就写着注释
「This one sits ON lime, so it flips to white」。

已按需求原样落地并加了判据（`r50check.py` 会断言 hover 底色 == 底板色、边框仍是绿的）。
**要不要保留 1px 边框、或者改成描边加粗 / 白底，请裁决。**

**AA. ~~`.gb-header__logo` 现在没有任何 hover 反馈~~ — 第五十二轮裁决：保持现状 ✅**

> **裁决（第五十二轮）**：需求方选「保持现状（没 hover）」。第四十九轮的点名作数，
> 与「可点击处必有 hover」的公约冲突是**有意接受**的。已写进 HANDOFF 的
> 「不要报成 bug 的清单」。**本条关闭。**

原文备查：

第 6 条后半句要求去掉 logo 的 hover。logo 是链到首页的 `<a>`，属于可点击元素，
本站公约「可点击处必有 hover」（全局 CLAUDE.md 铁律 13）对它是适用的。
已按需求去掉，连同那条已经没有用武之地的 `transition: trans(opacity)` 一起删。
**这是一处有意为之的例外，下一轮审计不要当 bug 修回去**（已同步进 HANDOFF 的「不要报成 bug」）。

**AB. ~~第 8 条的撤回只落到 95% 那组~~ — 已于第五十轮裁决：两组统一 ✅**

第四十三轮按需求「由 95→50」把 `.gb-science-card__value` 的手机值改成 36/40，
**并顺手删掉了 `--nutrient` 自己的同名覆盖**（当时两组同源了）。这一轮要「去掉 36/40」，
如果只是把那段删掉，两组会**一起**回到 56/44 —— 而 50% 那组的板 324:58044 写的就是 36/40。

所以本轮的做法是**把那段移到 `.gb-science-card--nutrient .gb-science-card__value` 上**，
不是删除：95% 组回到板值 56/44（板 228:5932），50% 组保持 36/40。
**若需求方本意是两组都回 56/44，说一声就改**，那样 50% 组会推翻它自己的板值。

**AC. reel 弹窗里播放图标的 hover 颜色是自定值**

第 5 条要 `.gb-rv-panel__video` 的 svg hover 变色。这整个 lightbox 本来就是「稿里没有」的
自建件（源码注释里写着），hover 态更是既不在稿里也不在 build spec 里。
本轮取 `$c-green`（本站的可交互绿），过渡 `$t-base` / `$ease-out`。**颜色与时长待裁决。**

⚠ 另外这是个**纯占位 div**，目前点它没有任何反应（真视频还没交付）。给不可点的元素加
hover 与公约相反，本轮按需求原样做了，等真视频接上就自然成立。

**AD. deco-bear 钉成 px 的两个副作用**

第 2 条要求 pc 端固定宽高。原来写的是 `width: 7.36%`，两处副作用：

1. **1440 窗口下它其实是 104.88px 不是 106** —— 7.36% 量的是内容宽（1425，扣掉滚动条），
   而 106 是板宽 1440 上的槽位。钉成 106px 之后按板值走，屏幕上会比之前**大 1.1px**。
2. **768–1280 这一档原来跟着百分比连续变化**，pc 一钉死就会在 1280/1281 处跳 94 → 106。
   本轮给这一档补了 `fluid(81.6px, 106px)` 的斜坡接上两头（判据里有 767/768、1280/1281
   两个接缝断言）。**这一档的取值是推算出来的，不是板值** —— 稿只有 390 和 1440 两块。

**AE. ~~Swiper 是 154KB~~ — 已于第五十轮裁决：开销值得，全站轮播都改 Swiper ✅**

第 3 条要求把 `.gb-product__gallery` 改成 Swiper。原来是约 60 行原生 JS。
`assets/swiper-bundle.min.js` 是 154KB（gzip 后约 45KB），挂在 5 个页面上，
且因为 `main.js` 不是 defer，它也不能 defer。行为已一比一对齐（见 CHANGELOG 第四十九轮）。
**若在意首屏体积，可换回原生实现或改用只含 core + effect-fade 的自建包**（后者需要构建步骤，
Shopify 的 `assets/` 不收子目录，当前没有）。

⚠ 缩略图轨**没有**改成 Swiper 的 thumbs 模块 —— 那会把它变成 transform 轨，丢掉
scroll-snap、overflow 滚动，以及它在 Lenis `PREVENT` 名单里的位置。它仍是普通按钮条。

**AF. ~~PDP 的图片 sticky 现在从 768 起，首页那块没跟~~ — 已于第五十轮裁决：每一页都钉 ✅**

第 7 条要 `gb-product__media` sticky。原来只在 `@include pc`（1281+）生效，
而两栏布局从 768 就开始了 —— 也就是说**任何 1280 及以下的笔记本上图片都不钉**，
这多半就是需求方看到的现象。本轮把 sticky 铺到 768+，偏移量跟着表头高度一起插值。
**首页（`.gb-product--lg`）和 reviews / our-story / how-gumi-works 上的同款区块没有加** ——
sticky 一直是 `--page`（PDP）专属，交接备注里写的也是 "PDP product image sticky"。
**要不要首页也钉，请裁决。**

### 第五十轮新增的待决事项（2026-08-28）

**AG. ~~全站轮播改 Swiper 之后，「无限循环」没了，换成了 rewind~~
— 第五十二轮裁决：reels 加卡换回 loop，expert 保持 rewind ✅**

需求方点名「轮播图都需要改为 swiper」。reels 横轨（4 页 × 5 张卡）与 expert 卡片轨
（reviews，3 张卡）原来都是**克隆式无限循环**：脚本把整组卡复制到 12 份，滚到边界再
悄悄跳回去，所以两侧永远有卡。

**Swiper 11 的 `loop` 做不到这件事** —— 它是把**现有的** slide 重新排序，不复制 DOM，
所以轨道上永远只有那几张卡。实测（index@1440，可见宽 1425，可放 4.3 张，只有 5 张）：

| 配置 | 卡片位置 | 右侧空档 |
|---|---|---|
| 旧的克隆循环 | −96 / 232 / 560 / 888 / 1216 | 0 |
| Swiper `loop: true` | −423 / −95 / 232 / 560 / 888 | **232.5px** |
| 本轮（居中 + 中间张起步 + `rewind`） | −95.5 / 232.5 / 560.5 / 888.5 / 1216.5 | 0 |

板上也没有循环：Reels Row 在 1440 画布上是 **1617 宽、x = −88**，正好是五张卡居中、
两侧各探出 88 —— 就是本轮这套排布。判据 `r50check.py` 第 9 节把 50 个「页 × 档位」
组合与改造前的几何逐点比过，**0 处差异（容差 0.7px）**。

**代价**：`rewind` 到头之后是**倒回**第一张（会看到整条轨道退回去），不是无缝续接。
**要恢复无缝无限循环，只能加卡** —— Swiper 的 loop 大约需要可见张数的两倍以上，
1440 处 4.3 张可见就意味着至少 9–10 张真实卡片。当前 reels 是 5 张灰占位、
expert 是 3 张重复文案的占位卡，**内容到位后这条可以重新评估**。

> **裁决（第五十二轮）**：需求方选「要无缝循环，得加卡」。**两条轨道代价不同，只做了一条**：
>
> - **`.gb-reels` 加卡 5 → 10、改跑 `loop`**。它全断点都是轨道，加卡不动布局。
>   新增的 5 张是第 1 张的副本（灰占位），HTML 里注明「不是内容、是给 loop 的料」，
>   已登记进「交付前必须替换的占位内容」。
> - **`.gb-expert__cards` 不动**（仍 3 张、仍 `rewind`）。它 **≥992 是三列网格**，
>   补到 9 张会把一行三张变成三行 —— 那是改桌面，超出本条授权。
>
> ⚠ **已知代价**：板上的取景是「五张居中、两侧各探出 88」（Reels Row 1617 / x = −88），
> 那是没有循环时的排布。loop 之后两侧永远盖满（1440 静止时左侧探出 416），
> **与稿不再一致** —— 这是无缝循环的固有代价。判据 `r53check` §2。**本条关闭。**

**AH. expert 轨在 768–991 的起点从「贴视口边」变成「贴视口边」（未变），但容器不再有 padding**

原来 `.gb-expert__cards` 在 ≤991 带 `padding-inline: var(--pad-x)`，而 `scroll-padding` 是 0，
所以卡片其实是**贴着视口边**吸附的 —— 那圈 padding 没有对齐任何东西。
Swiper 用 `clientWidth`（含 padding）量容器，带 padding 会让它以为地方比实际多，
991 处整组左移 24px、第三张被切。**本轮把 padding 去掉**，几何回到与改造前完全一致。
若设计方要的是「卡片对齐版心 gutter」（那会和 992 以上的三列网格对齐），
这是一处**真实的观感改动**，需要单独提，不能顺手做。

### 第五十一轮新增的待决事项（2026-08-28）

**AI. ~~三列的下界从 1281 降到 1201，那一段每列比组件想要的窄~~
— 第五十二轮裁决：保持 ✅**

> **裁决（第五十二轮）**：需求方选「保持（需求方点名的 1200）」。
> 「挤」不是本轮引入的（改前三列从 1281 起就吃不满），`rwd.py` 全绿。**本条关闭。**

原文备查：

需求方点名「1200 以下变 2 个」。落地之后 1201–1280 这一段从两列变成三列，
而 `.gb-science__cards` 的注释里原本记着一条推导：一列要装下 `.gb-bear-meter`
的 347 上限加两侧 32 的 padding = **411**，三列因此需要约 1361 的视口 ——
这正是当初把三列留在 pc（≥1281）的理由。

实测每列宽度（`.gb-science__cards` / `.gb-testimonial` / `.gb-story__inner` 三者几乎同宽）：

| 视口 | 每列 | `.gb-bear-meter` 实际宽 | 列态 |
|---|---|---|---|
| 1440 | 410.7 | 346.7（吃满 347）| 三列 |
| 1281 | 357.7 | 293.7 | 三列 |
| **1280** | **357.4** | **293.5** | **三列（本轮之前是两列）** |
| 1240 | 346.8 | 284.8 | 三列（本轮之前是两列）|
| **1201** | **336.5** | **276.3** | **三列（本轮之前是两列）** |
| 1200 | 411.5 | 347 | 两列 |

**「挤」不是本轮新引入的** —— 改前三列从 1281 起就已经吃不满：那里每列 357.7，
bear-meter 只有 293.7。本轮把这个区间从 [1281, ~1361] 往下扩到 [1201, ~1361]，
最窄处从 357.7 变成 336.5，**差 21.2px**。

⚠ 1201 → 1200 处有一个 336.5 → 411.5 的跳档，那是三列换两列的固有性质
（列数少了，每列自然变宽），不是断层。

`.gb-bear-meter` 的 `max-width: 347` 在这一段吃不满，会跟着列宽缩。
`rwd.py` 12 × 14 全绿（没有溢出、没有文字被裁），**是「挤」不是「坏」**。
若设计方觉得 1201–1280 的三列太窄，回退只要把这几个组件的
`@include tight` 换回 `@include tablet` + `@include narrow` 两块。

**AJ. ~~`.gb-product__cta` 的最大宽度没有给数值，本轮取 520~~
— 第五十二轮裁决：就用 520 + 居中 ✅**

> **裁决（第五十二轮）**：需求方选「就用 520 + 居中」。**本条关闭。**

原文备查：

需求写的是「767 以下 `.gb-product__cta` 加上最大宽度」，没有给数字。
本轮取 **520**，理由是 `.gb-product__media` 手机端的上限就是 520（第四十三轮同样是推算值，
待决 Q），按钮因此和它正上方的产品图同宽；390 处那一列本来就只有 350，稿上一个像素没变。
另外配了 `margin-inline: auto` —— 只加上限不居中的话，按钮会贴着左边，
而它上下的 `.gb-product__guarantee-note` / `.gb-product__guarantees` 都是居中的。
**数值和居中都是推算，请需求方确认。**

### 顺带发现 / 未修（第四十九轮）— 第五十二轮已修 ✅

> **第五十二轮已授权并修完**：解锁推迟到滑出结束，时长由抽屉自己用 `--modal-exit` 声明。
> ⚠ 实现时踩到一处：手机档的抽屉和桌面下拉是**两套实现**（桌面 `grid-template-rows`
> 收起、`$t-panel` 0.35s；手机 `fixed` + `translateX` 滑出、**`$t-drawer` 0.7s**），
> 第一版把声明挂在基础规则上、时长差了一倍，判据把它抓了出来。
> 判据 `tools/r53check.py`（30 条），活性自检两条路径都量出 700 → 685 的 15px 横跳。
> 下面是原文备查。

**手机抽屉关闭时有和第 4 条一模一样的抖动。**

第 4 条修的是弹窗：关闭时 `is-modal-open` 立刻摘掉，滚动条回来，而 `position: fixed` 的
弹窗盒子（包含块是视口）当场变窄，里面居中的 panel 在**还完全可见的淡出过程中**横跳。
`.gb-header__panel` 抽屉是同一个机制，实测（faq.html，700px 桌面窗口，滚动条 15px）：

| 时刻 | 抽屉宽 | 视口 clientWidth | 抽屉可见 |
|---|---|---|---|
| 打开 | 700 | 700 | 是 |
| 点关闭 +60ms | **685** | **685** | 是（还在滑出，opacity 1） |

抽屉在滑出的第一帧就窄了 15px，同时整页内容右跳 15px。只在 **767 以下的桌面窗口**
（把浏览器拖窄、或小尺寸笔记本）出现，真手机的 overlay 滚动条不占布局宽，测不出来。

**没修** —— 需求第 4 条点名的是 `gb-rv-modal`，抽屉是另一个组件（铁律 20）。
修法与弹窗同形：`header.set(false)` 里把 `is-menu-open` 的摘除推迟到抽屉滑出结束。
**一句话就能授权。**

### 交付前必须替换的占位内容

- **Reviews 专家卡的引用文案里有竞品名**：三张卡都写着「Grüns has everything I need…」，
  Grüns 是另一个软糖品牌，设计师直接抄了参考站文案。原样保留在 HTML 里并加了注释，
  **上线前必须换掉**。
- **Shipping 全页文案写的是美国配送**（Alaska、Hawaii、US Territories、$65 门槛），
  而 Gumi 是澳洲品牌。同样是设计稿的占位。
- **Privacy Policy 正文全是 lorem ipsum**。
- **reels 轨道的第 6–10 张卡是第 1 张的副本**（第五十二轮为了让 Swiper 的 `loop`
  有料可推而加的，见待决 AG）。它们**不是内容**，HTML 里有注释标着。
  真实 reels 到位后**整组替换**，最终张数由客户的内容决定 ——
  ⚠ 但**不能少于可见张数的两倍**（1440 处 4.3 张 → 至少 9 张），否则 loop 会在一侧留空，
  那时要么补够张数、要么退回 `rewind`。
- Get in Touch 的 **Enquiry Type 选项列表**稿里只给了「Contact Us」一个。现在的四项
  对应 header/footer 指向本页的四个链接（Partners & Influencers / Press Inquiries /
  Careers / Contact），并支持 `?type=` 预填（批注要求照搬 Funky 的做法）。需客户确认最终列表。

### 落地前需向设计方确认的
0. **手风琴展开动画只在 Chrome 系有**（`::details-content` + `interpolate-size`）。
   改成原生 `<details>` 是为了让开合不依赖 JS；Firefox / Safari 现在是瞬开瞬收。
8. **Shipping 桌面稿缺失** — 只有手机稿，**已按 Privacy Policy 的文本页布局实现**，需确认
9. **PDP 产品图 sticky 的吸顶偏移量** — 批注只说要 sticky，没给数值，现用 `top: 24px`
10. **营养标签弹窗开合时长**（0.4s）与曲线 — 稿中没有，是自定值（第三轮遗留）。
    ⚠ **出现方式已经反转过两次**：批注 401:31227 原本要「底部上滑」→ 第二十八轮按
    「所有弹窗只淡入淡出」全撤 → 第三十轮按需求方点名，**只在手机端（≤767）改回上滑**，
    桌面与另外两个弹窗仍是纯淡入淡出。再动之前先读这一条。
11. **平板（576–1280）没有设计稿**（第十六轮）—— 需求方 2026-08-21 把断点定成
    手机 ≤575 / 平板 576–1280 / PC ≥1281，但 Figma 只有 390 与 1440 两张板。
    这一档现在所有数值都是两张稿之间的**线性插值**（`fluid()`，576 处等于手机值、
    1281 处等于桌面值），`nutrition__band` 的四个量取的是中点。
    栅格按需求做成 3→2→1，两列的阈值是按列宽算出来的（science 744 / nutrition 704）。
    设计方若出平板稿，改的是插值的两个锚点，不用重排布局。

### 第五十三轮新开的（2026-08-30）

**三条需求本身要先裁决 —— 第五十四轮已按「按最新需求」的指示全部落地 ✅：**

- ~~**AQ. 第二组·2 与第一组·7 对撞**~~ — **第五十四轮按第二组（较新的一批）落地：
  回到正居中，`narrow` / `tablet` 两条覆盖整块删除。关闭，但 → 待决 I 重新打开**
  （现在与板的 *hangs slightly left of centre* 不一致）。
- ~~**AR. 第二组·5「promo-modal 到手机端才全屏」**~~ — **第五十四轮落地：全屏只到 ≤767，
  768–1280 显示手机板自己的 390×744 居中卡片（每个数都是板值，零自造）。关闭 → 新待决 AT**
  （这一档没有板，形态是取的）。
- ~~**AS. 第三组·4「select 改成 ul 点击下拉」**~~ — **第五十四轮落地：`main.js` 的
  `selectBox` 模块，渐进增强（原生 select 留作取值载体）。关闭 → 新待决 AV / AW。**

**已按需求落地，但取值或代价需要过目：**

- **AK. `.gb-deco-bear--b` 的 `top` 改成百分比的漂移量** — 需求点名要百分比，已落地，
  两档各按自己的板换算（pc `71.087%` = 408/573.94，narrow `87.211%` = 457/524.02），
  **板上分毫不差**。但 `.gb-footer-cta-wrap` 的高度随宽度与文案行数变，离开板宽后熊会漂：
  **1281 −7.4px / 767 −52.4px / 320 +55.8px**。768–1280 只能留 px（`fluid()` 不能插值百分比）。
  这正是源码里原本写明「top stays in px on purpose」的理由，**是有意推翻，不是回归**。
- **AL. `.gb-vs__table` 能不能一路全宽** — 现在只在 ≤575 全宽。`.gb-vs__bear` 是这个盒子的
  百分比、右缘落在它的 103.2% 处，去掉 cap 后 767 实测文档宽 771 > 视口 767。
  要一路全宽就得连熊一起重排 —— 那是改布局，不是还原。
- **AM. 抽屉 CTA 的 520px 是取的值** — 需求说「加上一个最大宽度」但没给数值。
  取 520 = `.gb-product__cta` 的同值（第五十一轮同样没给数值时取的）。390 板上够不到它。
  左对齐不居中：auto 外边距会把它推离左对齐的链接列。
- **AN. `.gb-science-card__text` 的 6px 该不该限定作用域** — 需求这一句没写作用域，
  而紧邻的上一句写了完整类链 `gb-science gb-science--cream gb-science--tight`，
  差别看起来是有意的，所以落在全局（index 3 张 + science 6 张都吃到）。
  若只想要 tight 那组，挪进作用域即可。
- **AO. 数字描边 0.145em 偏离板值** — 为消掉 `0` 字怀与 `50`/`%` 之间的白斑，
  半径从 Figma 的 7px（0.125em @56）提到 8.12px（0.145em），是**恰好归零**的最小值
  （0.125 → 272 个洞像素，0.135 → 61，0.145 → 0）。外轮廓因此粗 1.1px。
- **AP. 第三组·3 语义不明，未动手** — 「`gb-reviews--cream` 多了一个 `gb-testimonial`
  文本内容对齐 pc 端」。实测 `--cream` 版（our-story / how-gumi-works）确实是 **4 张**
  （index 是 3 张），第 4 张单独落到第二行居中；但**四张卡在 1440 / 1024 / 768 / 390
  四档下 `text-align` 全是 `center`，子元素也全是 center，与 pc 端没有任何差别**。
  是要「第 4 张对齐到第一列而不是居中」，还是别的意思？

### 第五十七轮新开的（2026-08-31）

**AY. favicon 的版式没有稿背书** — 设计源里没有 favicon 帧（`figma/` 里搜不到）。
落地取的是站上**现成的锁定组合**：footer 的 `.gb-footer__logo` 青柠字标（`#B5ED61`）
配它背后**实测**的底色 `#004128`（`$c-green-900`，不是 `$c-green`），字形四条 path
与 footer 逐字节相同。**由我定的只有版式**：64 方画布、字标占 78% 宽居中（给 iOS 圆角遮罩
留安全区）、不做圆角。
⚠ 另外**四个字母在 16px 标签页上基本读不出来**（只看得出深绿底 + 一抹青柠），
这是多字母字标做 favicon 的通病。**要不要改成只取一个 `G`？**

### 第五十六轮新开的（2026-08-31）

**AX. 手机端的波浪要不要跟着变浅** — 本轮按需求把白卡 `lip--v` 从 `left: -63px` 推到
`-100px`（咬进奶油半边由 63 变 26，占 126 盒子的 50% → 20.6%，与绿卡的 31 / 24.6% 接近）。
这条声明没有分档，所以 768–1440 全档同步；**≤767 画的是另一个元素 `lip--h`**
（`bottom: -48px`，咬痕 34.4，占 41.7%），需求没点名，按铁律 20 没动。
按同比例跟过去约是 `bottom: -65px`，**但那是换算值不是板值**。
**要不要一起变浅？要的话给个数，还是按比例换算？**

### 第五十五轮：需求方对第五十四轮五条的回复（2026-08-31）

回复原文：**「1 pc端居中 手机端去掉居中 / 2 糊了 / 3 忽略 / 4 一起改 / 5 忽略」**。

- ~~**I**~~ — **关闭，终版**：pc 端居中、手机端去掉居中，即回到第五十一轮那版。
  第五十四轮的「全档正居中」已撤回。⚠ 这条反转过三次，`r40check` / `r52check` /
  `r56check` 三处判据都写了「这是终版」，**下一轮别再按板或按 r56 改回去**。
  顺带补上了第五十一轮漏的一处：`tablet` 档也补 `margin-left: 0`，否则那一档一直在往右挂
  （768 +30.7 / 1024 +37.2 / 1280 +43.7）。
- **AT（重问）** — 需求方回「糊了」，是我第五十四轮没讲清楚。**要问的只有一句：
  768–1280（平板 / 小笔记本）这一档的邮件弹窗你想看到什么形态？**
  现在是手机板原尺寸 390×744 的居中卡片，1280 处约占视口 30%。**本轮未动。**
- ~~**AU**~~ — 需求方选择忽略，「手机端」继续按 ≤767。关闭。
- ~~**AV**~~ — **「一起改」，第五十五轮已落地**：国家码 select 走同一个 `selectBox` 的
  `bare` 变体（`get-in-touch` + `referral` 两页）。关闭。
- ~~**AW**~~ — 需求方选择忽略，不做首字母 typeahead。关闭。

⚠ **第五十五轮的验证是不完整的**（需求方叫停）：`rwd.py`（本轮动过两页 HTML，
按规矩必跑）、`r53check` / `r55check` / `scrolllock` / `revealcheck` / `hardbreaks` /
`platecheck` / `seamcheck` / `font-check.html` / 全站矩形波及比对**都没跑**。
**下一轮接手第一件事是补跑这些。** 已跑的清单见 CHANGELOG 第五十五轮。

### 第五十四轮新开的（2026-08-31）

**I（重新打开）. `.gb-promo-card__list` 的居中语义已反转两次** — 第四十轮给了固定
`margin-right: 15px`，第五十一轮按第一组·7 去掉 auto 左边距（结果是「居中后左移 7.5」，
正是板上的样子），第五十四轮按第二组·2 把两条覆盖整块删掉、回到**正居中**。
现在的实现**与板不一致**，是需求方点名的结果。**这次是终版吗？**

**AT. 768–1280 的弹窗形态是取的，不是板给的** — 这一档没有稿。落地取的是
「手机板 `285:19373` 自己的 390×744 居中卡片」，好处是堆叠布局里每一个值
（art 252 / 熊的 px 偏移 / 波浪 71 内缩 / space-between 的 32 间距 / 波浪节距 144.64）
**都停在它自己的板宽上，一个自造数值都没有**。代价是 1280 处这张卡只占视口约 30%。
另一条路是让桌面双栏提前到 768，但 1062 = 531 + 531 在 720 的可用宽里必须缩栏，
而熊的偏移是解在 531 上的 px，缩栏就被 `overflow: hidden` 切掉 —— **那才是自造数值**。
**要不要换形态？换的话需要 768–1280 的稿。**

**AU. 「手机端」判成 ≤767** — 需求原话（第二组·5、第三组·1）没给阈值。取的是本项目
一贯口径（值档 `narrow`，与第五十三轮三·1 同）。

**AV. 电话字段里的国家码 `<select>` 没改** — 需求只点名了 `gb-field__input--select`
（询问类型那一个），按铁律 20 没有顺手改另一个。**要不要一起改成同样的下拉？**

**AW. 自定义下拉没做首字母 typeahead** — 原生 `<select>` 输入首字母能跳到对应项，
自绘的没有。四个选项时影响很小，选项变多才明显。另外**主题化之后这个表单很可能由
app 接管**，这个控件届时可能作废 —— 现在补 typeahead 值不值，一起定。

## 已确立的规范（来自全局 CLAUDE.md + 既有 Shopify 项目）

### 项目文件结构（2026-08-19 用户指定，2026-08-21 第十五轮改为扁平）

```
Gumi-Brand/
├── *.html              # 页面文件放根目录（index.html / pdp.html / …）
├── assets/             # ⚠ 扁平，不建任何子目录（Shopify 主题 assets 的硬约束）
│   ├── customstyle.scss   # 样式源码，全站唯一一份
│   ├── customstyle.css    # 编译产物，勿手改
│   ├── main.js
│   ├── *.woff2            # 19 个：PP Palma / Figtree / Inter / Lexend / Playpen Sans
│   └── *.svg              # 44 个图标
├── images/             # ⚠ 图片在顶层，与 assets 平级（不是 assets/images）
├── figma/              # 设计源，只读，不进交付
├── tools/              # 验证脚本（cssnap / rwd / sect / shoot / webp），不进交付
└── docs/               # 项目文档，不进交付
```

⚠ **`assets/` 内不许建子目录**（2026-08-21 用户指定）。Shopify 主题的 `assets/`
本身就不接受子目录，现在的结构可以原样搬过去。

⚠ 与 Terra / EuroCave 的既往约定**有一处不同**：那两个项目图片在 `assets/images/`，
本项目图片提到**顶层 `images/`**。写路径时别顺手抄旧项目。
（上 Shopify 主题时 `images/` 也得进 `assets/`，届时一并处理。）

⚠ 样式**只有 `assets/customstyle.scss` 一个源文件**（原 7-1 的 36 个 partial 已合并）。
文件分「定义」「输出」两段，改之前先看文件头那段说明 —— 块的相对顺序是层叠依赖，
别随手挪动。编译：

```
npx sass@1.77.8 assets/customstyle.scss assets/customstyle.css --no-source-map
```

### 代码注释（2026-08-25 第三十轮需求方定）

- **一律英文，且极简**：只写「什么会坏、为什么」，不写推导过程与轮次叙事——
  那些进 [CHANGELOG.md](CHANGELOG.md)。判据是「将来别人读到这行，会不会因此少踩一次坑」，
  答案是否就删掉。
- ⚠ `font-check.html` 是**开发自检页**（标题写着「开发用，不属于站点」），
  页内中文是 UI 文案不是代码注释，本轮保留；要不要一起翻等需求方确认。

### 数值来源
- 所有数值 token（font-size / line-height / letter-spacing / 颜色 / padding / gap / radius / shadow）
  一律取 Figma 节点数据。截图只用于判断「有没有、谁在谁上面」。
- TEXT 节点必查 `characterStyleOverrides`；某视觉效果常由同层兄弟节点实现。

### 内容真实性
- 文本忠实设计源，占位保留占位或标 `TODO 待客户文案`，不编造。
- 图片一卡一图，不复用同一张冒充多张。

### 断点体系（2026-08-25 第二十九轮改制，改之前先读完这一节）

断点分成**两族**。**值档之间必须互斥**，布局阈值可以互相嵌套：

| 族 | mixin | 范围 | 管什么 |
|---|---|---|---|
| 值档 | `@include mobile` | ≤575 | 手机专属微调，叠在 narrow 之上（当前零使用者） |
| | `@include narrow` | **≤767** | 手机稿 390 的数值 + 手机排布 |
| | `@include tablet` | **768–1280** | `fluid()` 插值，没有设计稿 |
| | `@include pc` | ≥1281 | PC。桌面稿 1440 的值写在基础规则里 |
| 布局阈值 | `@include mid` | **≤991** | 卡片网格降一列 |
| | `@include stack` | ≤1024 | 稿里的两栏并排放不下，改上下堆叠 |
| | `@include tight` | ≤1200 | 版心开始吃紧 |

⚠ **值档不许重叠**。第十六~二十八轮的 `narrow`(≤768) 与 `tablet`(576–1280) 重叠了 193px，
胜出方由源码顺序决定，而顺序全站不一致 —— 130 个选择器在两边声明了同一属性，
表现是「同一行字的字号来自手机档、字距来自插值档」。第二十九轮改成互斥后一次性消失。

⚠ **布局阈值只准携带排布，不准携带数值**。`tight`/`stack` 里一旦写 `font-size`/`padding`，
就与 `tablet` 重叠、又把顺序依赖请回来，还会让 `narrow` 的手机值在 768 处硬跳
（`.gb-page-hero__title` 曾在 `tight` 里写死 48px，768 处从 30 直接跳上去）。
数值一律进 `tablet` 的 `fluid()`。**唯一例外是「跟着排布走的几何」** ——
如 `.gb-vs__pile` 的百分比定位：它相对的容器在堆叠时比桌面稿还宽，比例必须跟着布局切换，
且百分比**根本无法用 `fluid()`**（CSS `calc()` 不能用长度除以长度得到无单位进度）。

⚠ **布局阈值不要去对齐 767/1280**。一台 1194 宽的平板放得下稿里的两栏，硬堆成一列是把
好排版改坏；第十六轮第一版就是把 ≤1024/≤1200 全推到 1280，结果 1025–1280 从两栏变单列，
那是**改制引入的回归**。

⚠ **值档的交接必须由 `narrow` → `tablet` 完成，不能由 `mobile` → `tablet`**。
`mobile` 只到 575，而斜坡从 768 起，中间的 576–767 会掉回基础的桌面值 ——
`:root` 的 `--pad-x` 就这样短暂地在 576–767 用了 80px 的桌面留白（屏幕更窄留白反而更大），
第二十九轮修掉。

⚠ 平板数值一律写 `fluid($手机值, $桌面值)`，别拍脑袋填中间数。斜坡区间是 **768 → 1281**，
在 768 处精确等于手机值 —— 所以 `narrow` 与 `tablet` 的交接天然无缝，不需要额外对齐。
`fluid()` 的斜率**必须无单位** —— 写成 `26px * (100vw - 768px)` 是 px×px，量纲错了，
整条声明会被静默丢弃，表现是「字号原地不动、padding 掉回 0」而 DevTools 里那行看着完全正常。
⚠ **`fluid()` 只能用于 px**。百分比不行：CSS `calc()` 不能用长度除以长度得到无单位进度，
百分比量要么跟着排布走（写进布局阈值），要么换算成 px。

⚠ 改完**必须实测**跨宽度的计算值，不能看截图目测 —— 上面那个量纲错误就是目测判成
「已生效」、实测才抓出来的。判据脚本见下方「验证工具」。

### 类名前缀（2026-08-21 第十六轮定）

所有模块类都带 `gb-` 前缀（`.gb-hero` / `.gb-product__stage` / `.gb-btn` …），
为的是上 Shopify 主题后不跟主题自带样式和 app 注入的样式撞名。

**不带前缀的只有两类**，新增时照此办：
- 动效工具类：`wowo` / `animated` / `fadeIn*` / `zoomIn` / `zoomOut` / `bounceIn` / `delay-in-N`
  （main.js 与 Terra 的 wowo 约定挂在这些名字上）
- 状态类：`is-*` / `no-js` / `js`

### 验证工具（tools/，不进交付）

| 脚本 | 用途 | 什么时候必须跑 |
|---|---|---|
| `cssnap.py` | 全站 computed-style 快照 + diff（含伪元素，**11 档**：390/575/576/767/768/991/992/1024/1200/1280/1440，diff 分「不变量档 390·1440」与「观察档」两组报） | **改选择器名、搬 @media、合并文件** —— 这类改动 diff 产物无效，只能比计算值。⚠ 采样点必须落在改动的影响区内：改制前这里只有 390/1440，而断点改动的影响区是 576–1280，照跑会全绿却什么都没验到 |
| `r29edge.py` | 相邻宽度之间的数值跳变（读 `cssnap` 快照）。⚠ 不收 `height` —— 边界处排布本就重排，高度是结果不是原因，收了会有几千条被动差异埋掉真信号 | 动断点、动 `fluid()` 锚点之后 |
| `r29jump.py` | 同上，但直接开两个视口按 class 聚合，输出**能直接 grep 的选择器名**。⚠ 按 DOM 路径配对且跳过 `.gb-line-mask` 系列 —— `lineReveal` 在不同宽度拆出的行遮罩数量不同，按遍历顺序配对会整体错位、每条都是假的 | 同上，定位到具体规则时 |
| `rwd.py` | 11 页 × 10 档宽度：横向溢出 + 文字被祖先裁切 + **可滚容器是否登记给 Lenis** | 每动一次响应式；**每新增一个 `overflow-y:auto` 的容器** |
| `shoot.py --all` | 同上 + 全页截图 + 卡住的 `.wowo` | 收尾 |
| `sect.py` | 按区块截图（整页一万像素高看不动） | 要人眼看某个模块时 |
| `r19check.py` | 第十九轮 7 条任务的专项判据（弹窗时长 / 手风琴死区 / 图标 / 焦点 / 吸顶 / 弧 / 波浪色） | 动这七处中任意一处之后 |
| `r20check.py` | 第二十轮 8 条的专项判据（hero 按钮 / 星条 / 光晕尺寸 / logo 槽位 / 弧度 / 加号 / 箭头 / stats 波浪），全部与 Figma 数值比 | 动这八处中任意一处之后 |
| `make-hero-glow.py` | 按稿重建 `images/gumi-bear-front-glow.png`（glow 是稿里一条 26.2137 的 CENTER 描边，不是照片自带的） | 换 hero 熊照片、或要改光圈粗细 / 采样率时 |

用法：`python3 tools/cssnap.py before` → 改 → `python3 tools/cssnap.py after`
→ `python3 tools/cssnap.py diff before after`。
⚠ 快照前会把 `Math.random` 钉死 —— 不钉的话 word-pop / float-art 的抖动会让同一份 CSS
连采两次就有 260+ 处「差异」，真信号全被埋掉。

### CSS / 改动纪律
- 改样式前先定位真正胜出的规则（特异性 + 加载顺序），在原位就地改，不往文件末尾堆覆盖。
- 若主题存在 SCSS 源 → CSS 产物的构建链，**必须双写**或走差分移植，绝不全量重编译
  （funkyfood 的 `custom-style.css` 与 global-cke 的 `style.css` 都因此漂移过）。
- 响应式「拥挤」默认收窄 gap / padding 保持原布局，不重构；只有用户明确说改布局才动结构。

### 推送纪律（Shopify 多方共用环境）
- 改完只汇报「改了什么 + 怎么验证」，**等用户明确说推送才执行**。
- 一律 `--only <file>` 逐个列出 + `--nodelete`，**绝不裸跑 `shopify theme push`**（= 全量覆盖）。
- 推送前必做线上快照三方对比，有交集就停下人工处理。
- `config/settings_data.json` 由主题编辑器托管，非必要不推。

### Liquid 转义
- `<script>` 内用 `{{ value | json }}`；`onclick` 属性用 `{{ value | json | escape }}`；HTML 属性用 `{{ value | escape }}`。
- `<script>` 块里写 `| json | escape` 会双重编码损坏数据。

### 平滑滚动（2026-08-21 第十七轮定）

用 **Lenis 1.3.11**（MIT），vendored 在 `assets/lenis.min.js`，无构建步骤。
驱动代码是 `assets/main.js` 的 `smoothScroll` 模块。

- **`html { scroll-behavior }` 必须保持 `auto`**。Lenis 内部靠 `window.scrollTo` 落位，
  而按 CSSOM-View，`scrollTo` 是按元素自己的 `scroll-behavior` 执行的 —— 写 `smooth`
  就成了两层平滑套在一起，页面会飘会拖尾。
  老教程里那条 `.lenis.lenis-smooth { scroll-behavior: auto !important }` **自 Lenis 1.3
  起已失效**（不再输出 `lenis-smooth` 类），指望不上。
- **每新增一个 `overflow-y: auto` 的容器，就要登记进 `smoothScroll.PREVENT`**，
  否则 Lenis 把滚轮全吃掉、那个容器再也滚不动。现有三个：
  `.gb-product__thumbs`（只在桌面可滚）/ `.gb-header__panel`（只在手机可滚）/
  `.gb-nl-panel__body`。`tools/rwd.py` 逐视口盯这条 —— **单一视口验不全**。
- 触摸不接管（`syncTouch: false`），真机原生惯性比任何模拟都好。
- 两条关闭路径：`prefers-reduced-motion: reduce` 自动不启用；`<html data-no-smooth>` 手动关。
- 弹窗打开时 `lenis.stop()`（`modal.open` 里调），页面已被 `is-modal-open` 锁住，
  弹窗正文才滚得动；关闭时 `start()`。
- 站内锚点走 `lenis.scrollTo`，**不开 Lenis 自带的 `anchors`** —— 站里有 70 个
  `href="#"` 占位链接，交给它有把页面拉回顶部的风险。

### 波浪（scallop）的归属：放在所属 section 内（2026-08-21 第十八轮定，
### 尺寸机制 2026-08-24 第二十三轮改过，下面是当前状态）

**波浪是它所分隔的「上面那个 section」自己的最后一个子元素**，不是 `<main>` 下的
独立兄弟，后台里也不该是独立的一条——这条结构性决定第十八轮定的，没有变：

```html
<section class="gb-product gb-product--lg">
  …
  <div class="gb-scallop gb-scallop--edge gb-scallop--lg gb-scallop--white-to-mint"></div>
</section>
```

- **为什么归上面而不是下面**：`.gb-scallop--bleed`（nutrition 那道）要让上方模块的内容
  穿到波浪底下，跨不过模块边界。
- **尺寸写在波浪自己身上**（第二十三轮撤掉了「section 传 `--edge-w/band/h` 给波浪」
  这层——`.gb-scallop` / `.gb-scallop--lg` 本来就自给自足，直接读 `:root` 的
  `--sc-w/h` 或 `--sc-lg-w/h`）。section 只需要做一件事：把 `var(--sc-h)` 或
  `var(--sc-lg-h)` 加进自己的 `padding-bottom`（`calc(原值 + var(--sc-h))`），
  给波浪腾出一条自己的高度。多数模块「永远大瓦片」或「永远小瓦片」，直接把变量
  写死进 `padding-bottom`；同一个 class 在不同页面要求不同瓦片的（`.gb-page-hero--center`
  / `.gb-product` / `.gb-app-section` / `.gb-ingredients`），新增了 `--lg` 正交修饰类
  （`.gb-page-hero--lg` / `.gb-product--lg` / `.gb-app-section--lg` / `.gb-ingredients--lg`，
  跟 `.gb-scallop--lg` 是同一种命名思路，只覆盖 `padding-bottom`）。
  ⚠ **`position: relative` 也要自己补**——之前是 `.gb-sec-edge` 给的，删掉那层之后
  每个用到 `.gb-scallop--edge` 的 section 都要自己有这条，否则波浪的 `position:absolute`
  飘到更外层的定位祖先上去。
- 配色约定：`--wave-bg` 恒为**上方**那块的颜色、`--wave-fg` 恒为**下方**那块；
  `--down`（弧朝下）与 `--lg`（大瓦片）是两根正交的轴。
- ⚠ **不要写 `section + .gb-scallop` 这类相邻兄弟选择器** —— Shopify 给每个 section 套的
  `<div id="shopify-section-…">` 一来就全失配。（现在波浪在 section 内部，本来也不需要。）
- 页脚两道（`.gb-footer-cta-wrap` / `.gb-footer-wrap`）是**上边缘**、在流里，
  本来就在自己模块内，没有改；要做上边缘变体再加一组 `--edge-t`。

⚠ **改波浪尺寸机制或搬波浪时，路径式的 computed-style diff 无效**（节点一移动，
后面所有兄弟的下标整体错位，diff 比的是不同元素）。判据要换成与位置无关的不变量：
「波浪顶边到上方内容底边的间距」==该模块原来的 `padding-bottom` 数值、波浪底边
贴 section 底边（像素差 ≤0.6）、波浪宽度贴 section 宽度。脚本见 CHANGELOG 第二十三轮。

### 入场动效

**除下方「特殊动效」列出的以外，所有 fadeIn / fadeInUp 一律用 `wowo`，且尽量靠 class 控制。**

- 移植 Terra 的两份文件：`project/Terra/assets/scss/helpers/_animation.scss`
  + `script.js` 的 `morph.wowoSetup()` / `morph.wowo()`。不新写 IO 观察器、不引 AOS / animate.css / WOW.js。
- 用法：元素加 `class="wowo fadeInUp"`，错峰加 `delay-in-N`（N=1~20，每级 0.1s）。
  **写 HTML 时直接把 class 挂上**，不要在 JS 里逐个查询注入 —— 只有 CMS 导出不可改的 HTML
  才走 `wowoSetup()` 的选择器映射表。
- 可用动画名：`fadeIn` / `fadeInUp` / `fadeInDown` / `fadeInLeft` / `fadeInRight` /
  `zoomIn` / `zoomOut` / `bounceIn`；时长固定 0.7s，fadeInUp 位移 30px；**只播一次不可重播**。
- ⚠ 隐藏门**不再是无条件的**（第十四轮改）：规则是 `html.js .wowo { opacity: 0 }`，`js` 类由
  `<head>` 的内联脚本加、并在 `load`（或 4s 兜底）时若 `window.gumi` 不存在就摘掉。
  **只加 `js` 类是不够的** —— 那行内联脚本在 main.js 404 / 顶层抛异常时照样执行，门照样成立；
  必须以「main.js 的收尾导出存在」为条件。改加载顺序后仍要验首屏 opacity 回到 1。
- ⚠ **动效 class 挂在包裹容器上，不挂在 `<img>` / `<picture>` 上**（第十七轮）：
  `<picture>` 当不了定位/动效元素（默认 inline，改 display 又会把 `<source>` 提升成布局项），
  且图片自己常带 `transform`（如 hero 小熊的 7.92° 倾斜），两者叠在同一个元素上会互相覆盖。
  是 `<picture>` 就用一个 `<div>` 包起来，class 挂 div。
- ⚠ **首屏元素不挂 `wowo`**（第十四轮）：合成层上的 opacity 动画不产生 LCP 候选，
  要等 1500ms 移除 class 才登记，11 页 LCP 因此卡在 ~1550ms。折线以下照常用。

### 特殊动效（不走 wowo，单独实现）

| 模块 | 效果 | 参考 |
|---|---|---|
| Homepage **「60+ whole foods. No juicer, no fuss.」** | 小熊软糖图片**浮动** + **出现**动效 | https://www.cravburgers.shop/ **首屏的图案** |
| 全站文字出现效果 | 同参考站的文字入场手感 | 同上 |

对应批注 `401:29596` / `216:5903`（设计方原话就是让参考 cravburgers.shop 的汉堡浮动 + 文字淡入）。
落地前先实地看参考站的实现（浮动是否 GSAP、是否随鼠标视差、缓动曲线与周期），
**别凭「浮动」二字自己造一个 `@keyframes float`**。文字入场若与 wowo 手感一致则仍用 wowo，
只有确认不一致才单写。

### 实现边界：Shopify app 生成的内容不做（2026-08-19 用户指定）

搭前端时**跳过会由 Shopify app / metafield 产出的内容**，只留结构占位与样式外壳，不实现逻辑、不填假数据。
已核实设计稿中受此影响的区块：

| 页面 | 区块 | 稿中表现 | 处理 |
|---|---|---|---|
| PDP `324:52658` / `324:53792` | **订阅选购** | Autoship and Save / Subscribe & Save / One Time Purchase / Delivers every 4 Weeks / How subscription works | 由订阅 app 渲染，前端不做选项逻辑与价格计算 |
| PDP | **产品详细信息 accordion** | Why Gumi / Ingredients & Allergies / Science / Directions | 内容走 metafield/app，前端只做 accordion 壳 |
| PDP | **评论区** | Real Customer Reviews 4.76 / Based on 123,000 reviews / 5 张评论卡 / 点赞点踩计数 / See More Reviews | 由评论 app 渲染，前端不做 |
| **Reviews 页** `324:63924` / `324:64961` | 整页评论列表 | 同上 | 由评论 app 渲染，前端不做 |
| 全站 header | **Trustpilot 徽章** | `Excellent` / `Truspilot` | 第三方嵌入 |

⚠ 连带作废：批注 `401:31223`（评论支持选传图、点赞点踩排序）**属于评论 app 的能力，本次不实现**。
⚠ 仍要做的：**营养标签弹窗**（`401:31227` 底部上滑）不是 app 内容，是自定义模块，照做。
⚠ PDP 页脚「And Last Questions?」的 6 条 accordion 稿中就是 `Accordion Closed` / `Text here` 占位 ——
按铁律 3 **保留占位**，不编造问答。

## 设计源现状（已全量落盘）

| 项 | 数量 |
|---|---|
| 页面稿 | 11 组（Desktop 1440 ↔ Mobile 390），Shipping 缺 desktop |
| 组件与状态稿 | 导航 4、购物车 4、弹窗 9、其他 3 |
| 完整节点树 | 42 个 frame + 30 条批注 |
| 参考截图 | 72 张 PNG @1x |
| 字体样式组合 | 139 种 |
| 颜色 | 95 种（含描边） |
| 圆角 | 60 种 |
| 阴影 | 19 种 |
| 文案条目 | 71 份逐页清单（`docs/copy/`） |
| 字体文件 | `assets/` 共 19 个 woff2（804K），其中 13 个被 `customstyle.scss` 的 `@font-face` 段引用 |

**⚠ 33 个 frame 含 `characterStyleOverrides`**（同段文字里换字体/色/字号）——
实现时必须逐字符查 `characterStyleOverrides` + `styleOverrideTable`，不能只取 TEXT 顶层 style。
清单在 [DESIGN-TOKENS.md](DESIGN-TOKENS.md) 末尾。

**品牌视觉**：字体 **PP Palma**（主，3298 次）/ **Inter**（409）/ **Lexend**（108）/ **Playpen Sans**（90）；
主色 `#B5ED61` 青柠绿、`#005635` 深绿、`#011307` 近黑绿、`#FAF9F8` 米白。

字体文件与 `@font-face` 已就位于 `assets/customstyle.scss`（原 `base/_fonts.scss` 段）：

- Inter / Lexend 是**可变字体**（一个文件覆盖全字重，md5 实测同一文件）→ `font-weight` 写范围
- Playpen Sans 是**静态实例**，只有 600 → `font-weight` 写单值，要别的字重得另外下载
- PP Palma 无授权文件，Figtree 占位；**全站走 `$font-brand-stack`，换字体只改这一处**
- 稿中的 `SF Pro Text` 不是字体需求，是 SF Symbols 图标占位（Trustpilot 星标），导 SVG

## 阻塞

1. **PP Palma 300（FizzyLight）仍是试用包** —— `PP Palma - Free For Personal Use v1.0/` 的
   EULA 明确排除商业用途，且不随 git 仓库分发。400/500/800 第二十六轮起已换成客户授权文件
   （`assets/PPPalma-Regular/FizzyMedium/FizzyHeavy.woff2`，大写驼峰命名）。
   **上线前必须补齐 300**。切换点是 `customstyle.scss` 的四条 `@font-face`，只改 `src`。
2. **Shopify 店铺 / 主题基底 / 接入方式未定** —— 只影响后续主题化，静态阶段不受影响。
3. **Figma `/v1/images` 账号级限流**（2026-08-26 实测 429，`retry-after` ≈ 2.8 天 → 约 08-29 解除）。
   ⚠ 限流只打在**导出端点**：`/v1/files/nodes`（带 `geometry=paths` 可自生成 SVG）与
   `/v1/files/:key/images`（image fill 原图映射）都不受影响，撞限流先走这两条，别直接等。
   可用 PAT：**ly-design / `dev@mockuptocode.com`**；`~/.claude/settings.json` 里的 `figd_NR7GZ…` 已耗尽。
4. **768–1280 这一档始终没有设计稿**（常驻）。所有值要么是 390→1281 的 `fluid()` 斜坡，
   要么是行为约束，没有一个是板值。

## 历史

逐轮进度表、已关闭的待决事项、已解除的旧阻塞全部移到
[PROJECT-STATUS-ARCHIVE.md](PROJECT-STATUS-ARCHIVE.md)（2026-08-27 分卷，原文未改一字）。
